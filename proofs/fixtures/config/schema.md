# Service configuration contract

`service` must be a non-empty string. `retries` must be an integer from 0 through 5. `timeout_seconds` must be an integer from 1 through 120. `regions` must be a non-empty array of distinct strings. No other top-level keys are permitted.
